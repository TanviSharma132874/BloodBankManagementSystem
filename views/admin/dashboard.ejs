<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard - Blood Bank</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    body {
      min-height: 100vh;
      background: linear-gradient(135deg, #f1faee 0%, #e63946 100%);
      position: relative;
      color: #333;
      overflow-x: hidden;
    }
    /* Animated SVG wave background */
    .wave-bg {
      position: absolute;
      top: 0; left: 0; width: 100%; height: 220px; z-index: 0;
      pointer-events: none;
    }
    /* Glassmorphism card effect */
    .glass-card {
      background: rgba(255,255,255,0.7);
      border-radius: 18px;
      box-shadow: 0 8px 32px rgba(230,57,70,0.18);
      backdrop-filter: blur(8px);
      border: 1px solid rgba(230,57,70,0.12);
    }
    /* Sidebar */
    nav {
      width: 220px;
      min-height: 100vh;
      background: linear-gradient(180deg, #e63946 60%, #f1faee 100%);
      color: #fff;
      padding: 24px 18px;
      position: fixed;
      left: 0; top: 0; bottom: 0;
      z-index: 10;
      box-shadow: 0 4px 24px rgba(230,57,70,0.12);
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      align-items: center;
    }
    nav .logo {
      width: 80px;
      margin-bottom: 18px;
      filter: drop-shadow(0 2px 8px #b71c1c44);
    }
    nav h1 {
      font-size: 22px;
      margin-bottom: 30px;
      text-align: center;
      font-weight: bold;
      letter-spacing: 2px;
    }
    nav a {
      text-decoration: none;
      color: #fff;
      padding: 10px 15px;
      margin: 5px 0;
      display: block;
      border-radius: 8px;
      transition: 0.3s;
      font-size: 1.1rem;
    }
    nav a:hover {
      background: #b71c1c;
      transform: scale(1.05);
    }
    /* Main Content */
    .main-content {
      margin-left: 240px;
      padding: 32px 24px 0 24px;
      position: relative;
      z-index: 1;
    }
    /* Motivational Banner */
    .motivation {
      background: linear-gradient(90deg, #e63946 60%, #f1faee 100%);
      color: #fff;
      border-radius: 16px;
      box-shadow: 0 4px 24px rgba(230,57,70,0.12);
      padding: 24px 32px;
      margin-bottom: 32px;
      display: flex;
      align-items: center;
      gap: 24px;
      font-size: 1.3rem;
      font-weight: 500;
    }
    .motivation .bi {
      font-size: 2.5rem;
      margin-right: 12px;
      animation: pulse 1.2s infinite alternate;
    }
    @keyframes pulse {
      0% { transform: scale(1); color: #fff; }
      100% { transform: scale(1.15); color: #ffd6d6; }
    }
    /* Dashboard Cards */
    .cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 28px;
      margin-top: 30px;
    }
    .card {
      background: rgba(255,255,255,0.85);
      padding: 28px 18px;
      border-radius: 18px;
      box-shadow: 0 8px 32px rgba(230,57,70,0.12);
      text-align: center;
      position: relative;
      transition: box-shadow 0.3s, transform 0.3s;
    }
    .card:hover {
      box-shadow: 0 12px 40px rgba(230,57,70,0.22);
      transform: translateY(-4px) scale(1.03);
    }
    .card h3 {
      margin-bottom: 10px;
      font-size: 22px;
      color: #e63946;
      font-weight: bold;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
    }
    .card p {
      font-size: 2.1rem;
      font-weight: bold;
      color: #222;
      margin-bottom: 0;
    }
    .card .bi {
      font-size: 2.2rem;
      color: #e63946;
      margin-bottom: 8px;
      animation: pulse 1.2s infinite alternate;
    }
    .card .btn {
      margin-top: 10px;
      border-radius: 20px;
      font-weight: 500;
      font-size: 1.1rem;
    }
    /* Floating Action Button */
    .fab {
      position: fixed;
      bottom: 32px;
      right: 32px;
      z-index: 100;
      background: linear-gradient(135deg, #e63946 60%, #f1faee 100%);
      color: #fff;
      border: none;
      border-radius: 50%;
      width: 64px;
      height: 64px;
      box-shadow: 0 4px 24px rgba(230,57,70,0.18);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2.2rem;
      cursor: pointer;
      transition: background 0.3s, transform 0.3s;
    }
    .fab:hover {
      background: linear-gradient(135deg, #e63946 80%, #f1faee 100%);
      transform: scale(1.08);
    }
    /* Footer */
    .footer {
      margin-top: 48px;
      text-align: center;
      color: #e63946;
      font-size: 1.1rem;
      opacity: 0.8;
    }
    @media (max-width: 900px) {
      nav { width: 100px; padding: 12px 6px; }
      .main-content { margin-left: 110px; padding: 18px 6px 0 6px; }
      .motivation { font-size: 1rem; padding: 12px 10px; gap: 10px; }
      .cards { gap: 12px; }
    }
  </style>
</head>
<body>


  <!-- SVG Wave Background -->
  <div class="wave-bg">
    <svg viewBox="0 0 1440 220" fill="none" xmlns="http://www.w3.org/2000/svg" style="width:100%;height:100%;">
      <path fill="#e63946" fill-opacity="0.18" d="M0,160 C480,220 960,80 1440,160 L1440,0 L0,0 Z"></path>
      <path fill="#e63946" fill-opacity="0.12" d="M0,120 C480,180 960,40 1440,120 L1440,0 L0,0 Z"></path>
    </svg>
  </div>
  <nav>
    <img src="/images/blood-donation.png" alt="Blood Bank Logo" class="logo">
    <h1>🩸 Blood Bank Admin</h1>
    <div style="width:100%">
      <a href="/adminDashboard"><i class="bi bi-speedometer2"></i> Dashboard</a>
      <a href="/admin/donors"><i class="bi bi-people-fill"></i> Donor List</a>
      <a href="/admin/stock"><i class="bi bi-droplet"></i> Stock</a>
      <a href="/admin/recipients"><i class="bi bi-hospital"></i> Patient List</a>
  <a href="/admin/requests"><i class="bi bi-person-lines-fill"></i> Users & Their Data</a>
      <form action="/admin/logout" method="POST" style="margin-top:18px;"><button type="submit" class="btn btn-danger w-100"><i class="bi bi-box-arrow-right"></i> Logout</button></form>
    </div>
  </nav>
  <div class="main-content">
    <div class="motivation">
      <i class="bi bi-heart-pulse-fill"></i>
      <span>"Every drop counts. Your leadership saves lives!"</span>
    </div>
    <div class="row justify-content-center">
      <div class="col-lg-11">
        <div class="glass-card p-4 mb-4">
          <div class="row g-0 align-items-center">
            <div class="col-md-5 d-flex align-items-center justify-content-center">
              <img src="/images/14870481.png" alt="Blood donation banner" class="img-fluid rounded-4" style="max-height:220px;">
            </div>
            <div class="col-md-7">
              <h2 class="mb-3 fw-bold text-center" style="color:#e63946;">Welcome, Admin!</h2>
              <p class="lead text-center mb-2">Manage donors, recipients, and blood stock here.<br><span style="color:#e63946; font-weight:bold;">Your actions save lives!</span></p>
              <div class="cards mt-4">
                <div class="card">
                  <span class="bi bi-people-fill"></span>
                  <h3>Total Donors</h3>
                  <p><%= typeof totalDonors !== 'undefined' ? totalDonors : 0 %></p>
                </div>
                <div class="card">
                  <span class="bi bi-droplet"></span>
                  <h3>Available Units</h3>
                  <p><%= typeof availableUnits !== 'undefined' ? availableUnits : 0 %></p>
                </div>
                <div class="card">
                  <span class="bi bi-person"></span>
                  <h3>User</h3>
                  <p><%= typeof pendingRequests !== 'undefined' ? pendingRequests : 0 %></p>
                  <a href="/admin/requests" class="btn btn-warning">View Users</a>
                </div>
                <div class="card">
                  <span class="bi bi-calendar-event"></span>
                  <h3>Upcoming Camps</h3>
                  <p><%= typeof upcomingCamps !== 'undefined' ? upcomingCamps : 0 %></p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <!-- Floating Action Button -->
    <a href="/admin/stock" class="fab" title="Add Stock"><i class="bi bi-plus-circle"></i></a>
    <div class="footer">Powered by Blood Bank Management System &copy; 2025</div>
  </div>
