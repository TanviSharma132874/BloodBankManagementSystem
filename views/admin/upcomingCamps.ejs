<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upcoming Blood Donation Camps - Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        body {
            min-height: 100vh;
            background: linear-gradient(135deg, #f1faee 0%, #e63946 100%);
            color: #333;
        }
        nav {
            width: 220px;
            min-height: 100vh;
            background: linear-gradient(180deg, #e63946 60%, #f1faee 100%);
            color: #fff;
            padding: 24px 18px;
            position: fixed;
            left: 0; top: 0; bottom: 0;
            z-index: 10;
            box-shadow: 0 4px 24px rgba(230,57,70,0.12);
        }
        nav .logo {
            display: flex;
            align-items: center;
            font-size: 18px;
            font-weight: 600;
            margin-bottom: 30px;
        }
        nav .logo i {
            font-size: 24px;
            margin-right: 10px;
        }
        nav ul {
            list-style: none;
        }
        nav ul li {
            margin-bottom: 8px;
        }
        nav ul li a {
            color: #fff;
            text-decoration: none;
            display: flex;
            align-items: center;
            padding: 12px 16px;
            border-radius: 8px;
            transition: background 0.3s;
        }
        nav ul li a:hover {
            background: rgba(255,255,255,0.1);
        }
        nav ul li a i {
            margin-right: 10px;
            width: 20px;
        }
        .main-content {
            margin-left: 220px;
            padding: 30px;
        }
        .glass-card {
            background: rgba(255,255,255,0.9);
            border-radius: 18px;
            box-shadow: 0 8px 32px rgba(230,57,70,0.18);
            backdrop-filter: blur(8px);
            border: 1px solid rgba(230,57,70,0.12);
            padding: 30px;
        }
        .camp-card {
            background: rgba(255,255,255,0.95);
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 25px;
            box-shadow: 0 6px 25px rgba(230,57,70,0.15);
            border-left: 5px solid #e63946;
            transition: transform 0.3s, box-shadow 0.3s;
        }
        .camp-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 35px rgba(230,57,70,0.25);
        }
        .camp-title {
            color: #e63946;
            font-weight: 600;
            font-size: 22px;
            margin-bottom: 15px;
        }
        .camp-info {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 15px;
        }
        .info-item {
            display: flex;
            align-items: center;
            margin-bottom: 10px;
        }
        .info-item i {
            color: #e63946;
            font-size: 18px;
            margin-right: 12px;
            width: 25px;
        }
        .info-item strong {
            color: #333;
            margin-right: 8px;
        }
        .camp-description {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 10px;
            border-left: 3px solid #e63946;
            margin-top: 15px;
        }
        .no-camps {
            text-align: center;
            padding: 60px 20px;
            color: #6c757d;
        }
        .no-camps i {
            font-size: 64px;
            margin-bottom: 20px;
            color: #e63946;
        }
        .btn-back {
            background: #e63946;
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 8px;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            margin-bottom: 30px;
            transition: background 0.3s;
        }
        .btn-back:hover {
            background: #d32535;
            color: white;
        }
        .btn-back i {
            margin-right: 8px;
        }
        .status-badge {
            background: #28a745;
            color: white;
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 500;
            text-transform: uppercase;
        }
    </style>
</head>
<body>
    <!-- Sidebar Navigation -->
    <nav>
        <div class="logo">
            <i class="bi bi-droplet-fill"></i>
            Blood Bank Admin
        </div>
        <ul>
            <li><a href="/adminDashboard"><i class="bi bi-speedometer2"></i>Dashboard</a></li>
            <li><a href="/donorList"><i class="bi bi-people"></i>Donor List</a></li>
            <li><a href="/stockList"><i class="bi bi-droplet"></i>Stock</a></li>
            <li><a href="/admin/recipients"><i class="bi bi-clipboard-heart"></i>Patient List</a></li>
            <li><a href="/admin/requests"><i class="bi bi-person-plus"></i>Users & Their Data</a></li>
            <li><a href="/admin/camps"><i class="bi bi-calendar-event"></i>Manage Camps</a></li>
            <li style="margin-top: 40px;"><a href="/logout"><i class="bi bi-box-arrow-right"></i>Logout</a></li>
        </ul>
    </nav>

    <!-- Main Content -->
    <div class="main-content">
        <a href="/adminDashboard" class="btn-back">
            <i class="bi bi-arrow-left"></i>
            Back to Dashboard
        </a>

        <div class="glass-card">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2 class="text-danger fw-bold mb-0">
                    <i class="bi bi-calendar-event me-2"></i>
                    Upcoming Blood Donation Camps
                </h2>
                <span class="status-badge">
                    <i class="bi bi-clock me-1"></i>
                    <%= upcomingCamps.length %> Active
                </span>
            </div>

            <% if (upcomingCamps && upcomingCamps.length > 0) { %>
                <% upcomingCamps.forEach((camp, index) => { %>
                    <div class="camp-card">
                        <div class="camp-title">
                            <i class="bi bi-heart-fill me-2"></i>
                            <%= camp.name %>
                        </div>
                        
                        <div class="camp-info">
                            <div>
                                <div class="info-item">
                                    <i class="bi bi-calendar3"></i>
                                    <strong>Date:</strong>
                                    <%= new Date(camp.date).toLocaleDateString('en-US', { 
                                        weekday: 'long', 
                                        year: 'numeric', 
                                        month: 'long', 
                                        day: 'numeric' 
                                    }) %>
                                </div>
                                <div class="info-item">
                                    <i class="bi bi-clock"></i>
                                    <strong>Time:</strong>
                                    <%= camp.time %>
                                </div>
                                <div class="info-item">
                                    <i class="bi bi-geo-alt-fill"></i>
                                    <strong>Location:</strong>
                                    <%= camp.location %>
                                </div>
                            </div>
                            <div>
                                <div class="info-item">
                                    <i class="bi bi-person-badge"></i>
                                    <strong>Organizer:</strong>
                                    <%= camp.organizer %>
                                </div>
                                <div class="info-item">
                                    <i class="bi bi-telephone"></i>
                                    <strong>Contact:</strong>
                                    <a href="tel:<%= camp.contact %>" class="text-primary"><%= camp.contact %></a>
                                </div>
                                <div class="info-item">
                                    <i class="bi bi-calendar-check"></i>
                                    <strong>Status:</strong>
                                    <span class="text-success fw-semibold">
                                        <%= camp.status.charAt(0).toUpperCase() + camp.status.slice(1) %>
                                    </span>
                                </div>
                            </div>
                        </div>

                        <% if (camp.description) { %>
                            <div class="camp-description">
                                <strong><i class="bi bi-info-circle me-2"></i>Description:</strong>
                                <p class="mb-0 mt-2"><%= camp.description %></p>
                            </div>
                        <% } %>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="no-camps">
                    <i class="bi bi-calendar-x"></i>
                    <h4>No Upcoming Camps</h4>
                    <p>There are currently no upcoming blood donation camps scheduled.</p>
                    <a href="/admin/camps/add" class="btn btn-danger mt-3">
                        <i class="bi bi-plus-circle me-2"></i>
                        Schedule New Camp
                    </a>
                </div>
            <% } %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>