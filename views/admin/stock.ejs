<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blood Stock Management - Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            min-height: 100vh;
            background: linear-gradient(135deg, #f1faee 0%, #e63946 100%);
            position: relative;
            overflow-x: hidden;
        }
        
        /* Animated background shapes */
        .bg-shapes {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            pointer-events: none; z-index: 0;
        }
        
        .shape {
            position: absolute;
            border-radius: 50%;
            background: rgba(230, 57, 70, 0.1);
            animation: float 6s ease-in-out infinite;
        }
        
        .shape:nth-child(1) { width: 80px; height: 80px; top: 10%; left: 20%; animation-delay: 0s; }
        .shape:nth-child(2) { width: 120px; height: 120px; top: 70%; left: 80%; animation-delay: 2s; }
        .shape:nth-child(3) { width: 60px; height: 60px; top: 50%; left: 10%; animation-delay: 4s; }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(180deg); }
        }
        
        .container {
            position: relative;
            z-index: 1;
        }
        
        /* Header Section */
        .header-section {
            background: linear-gradient(135deg, #e63946 0%, #f1faee 100%);
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(230, 57, 70, 0.2);
            padding: 30px;
            margin-bottom: 30px;
            color: white;
            position: relative;
            overflow: hidden;
        }
        
        .header-section::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 100%;
            height: 100%;
            background: url('/images/blood.png') no-repeat center;
            background-size: 200px;
            opacity: 0.1;
            animation: rotate 20s linear infinite;
        }
        
        @keyframes rotate {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }
        
        .header-content {
            position: relative;
            z-index: 2;
        }
        
        .stats-cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin: 30px 0;
        }
        
        .stat-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 20px;
            text-align: center;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            border: 1px solid rgba(230, 57, 70, 0.1);
        }
        
        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(230, 57, 70, 0.2);
        }
        
        .stat-icon {
            font-size: 2.5rem;
            color: #e63946;
            margin-bottom: 10px;
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }
        
        /* Main Stock Table */
        .stock-container {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(15px);
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
            padding: 30px;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        
        .blood-group-card {
            background: linear-gradient(135deg, #fff 0%, #f8f9fa 100%);
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 20px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
            transition: all 0.3s ease;
            border-left: 5px solid #e63946;
            position: relative;
            overflow: hidden;
        }
        
        .blood-group-card::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 100px;
            height: 100px;
            background: url('/images/blood-donation.png') no-repeat center;
            background-size: 50px;
            opacity: 0.05;
        }
        
        .blood-group-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(230, 57, 70, 0.15);
            border-left: 5px solid #d63384;
        }
        
        .blood-type {
            font-size: 2.5rem;
            font-weight: bold;
            color: #e63946;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
        }
        
        .unit-count {
            font-size: 1.8rem;
            font-weight: 600;
            color: #495057;
        }
        
        .unit-label {
            color: #6c757d;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .status-indicator {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            display: inline-block;
            margin-right: 8px;
            animation: blink 2s infinite;
        }
        
        .status-high { background-color: #28a745; }
        .status-medium { background-color: #ffc107; }
        .status-low { background-color: #dc3545; }
        
        @keyframes blink {
            0%, 50% { opacity: 1; }
            51%, 100% { opacity: 0.3; }
        }
        
        .action-buttons {
            display: flex;
            gap: 10px;
            margin-top: 15px;
        }
        
        .btn-custom {
            border-radius: 25px;
            padding: 8px 20px;
            font-weight: 500;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
        }
        
        .btn-add {
            background: linear-gradient(135deg, #28a745, #20c997);
            color: white;
        }
        
        .btn-add:hover {
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(40, 167, 69, 0.3);
        }
        
        .btn-update {
            background: linear-gradient(135deg, #17a2b8, #6f42c1);
            color: white;
        }
        
        .btn-update:hover {
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(23, 162, 184, 0.3);
        }
        
        /* Floating Action Button */
        .fab {
            position: fixed;
            bottom: 30px;
            right: 30px;
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, #e63946, #dc3545);
            border-radius: 50%;
            box-shadow: 0 5px 20px rgba(230, 57, 70, 0.4);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.5rem;
            text-decoration: none;
            transition: all 0.3s ease;
            z-index: 1000;
        }
        
        .fab:hover {
            transform: scale(1.1);
            box-shadow: 0 8px 30px rgba(230, 57, 70, 0.6);
            color: white;
        }
        
        /* Emergency Alert */
        .emergency-alert {
            background: linear-gradient(135deg, #dc3545, #e63946);
            color: white;
            border-radius: 15px;
            padding: 20px;
            margin-bottom: 20px;
            animation: emergencyPulse 3s infinite;
        }
        
        @keyframes emergencyPulse {
            0%, 100% { box-shadow: 0 0 5px rgba(220, 53, 69, 0.5); }
            50% { box-shadow: 0 0 20px rgba(220, 53, 69, 0.8); }
        }
    </style>
</head>
<body>
    <!-- Animated Background Shapes -->
    <div class="bg-shapes">
        <div class="shape"></div>
        <div class="shape"></div>
        <div class="shape"></div>
    </div>

    <div class="container py-4">
        <!-- Header Section -->
        <div class="header-section">
            <div class="header-content">
                <div class="row align-items-center">
                    <div class="col-md-8">
                        <h1 class="display-5 fw-bold mb-3">
                            <i class="bi bi-droplet-fill me-3"></i>
                            Blood Stock Management
                        </h1>
                        <p class="lead mb-0">Monitor and manage blood inventory efficiently</p>
                    </div>
                    <div class="col-md-4 text-end">
                        <img src="/images/14870481.png" alt="Blood management" class="img-fluid rounded-3" style="max-height: 120px;">
                    </div>
                </div>
            </div>
        </div>

        <!-- Quick Stats -->
        <div class="stats-cards">
            <div class="stat-card">
                <div class="stat-icon">
                    <i class="bi bi-droplet-fill"></i>
                </div>
                <h3 class="h4 mb-1">Total Types</h3>
                <div class="h2 text-primary"><%= stock.length %></div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">
                    <i class="bi bi-bar-chart-fill"></i>
                </div>
                <h3 class="h4 mb-1">Total Units</h3>
                <div class="h2 text-success">
                    <%= stock.reduce((total, item) => total + parseInt(item.units || 0), 0) %>
                </div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">
                    <i class="bi bi-exclamation-triangle-fill"></i>
                </div>
                <h3 class="h4 mb-1">Low Stock</h3>
                <div class="h2 text-warning">
                    <%= stock.filter(item => parseInt(item.units || 0) < 10).length %>
                </div>
            </div>
        </div>

        <!-- Emergency Alert for Low Stock -->
        <% if (stock.some(item => parseInt(item.units || 0) < 5)) { %>
        <div class="emergency-alert">
            <div class="d-flex align-items-center">
                <i class="bi bi-exclamation-triangle-fill fs-2 me-3"></i>
                <div>
                    <h5 class="mb-1">Critical Stock Alert!</h5>
                    <p class="mb-0">Some blood types have critically low inventory. Immediate action required.</p>
                </div>
            </div>
        </div>
        <% } %>

        <!-- Stock Cards -->
        <div class="stock-container">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h3 class="fw-bold text-primary">
                    <i class="bi bi-clipboard-data me-2"></i>
                    Current Blood Inventory
                </h3>
                <a href="/addStock" class="btn btn-custom btn-add">
                    <i class="bi bi-plus-circle me-2"></i>Add New Stock
                </a>
            </div>

            <div class="row">
                <% stock.forEach(function(item) { 
                    let statusClass = 'status-high';
                    let statusText = 'Good Stock';
                    let units = parseInt(item.units || 0);
                    
                    if (units < 5) {
                        statusClass = 'status-low';
                        statusText = 'Critical';
                    } else if (units < 10) {
                        statusClass = 'status-medium';
                        statusText = 'Low Stock';
                    }
                %>
                <div class="col-md-6 col-lg-4 mb-4">
                    <div class="blood-group-card">
                        <div class="d-flex justify-content-between align-items-start mb-3">
                            <div class="blood-type"><%= item.bloodGroup %></div>
                            <span class="badge bg-primary rounded-pill">Blood Type</span>
                        </div>
                        
                        <div class="text-center mb-3">
                            <div class="unit-count"><%= item.units %></div>
                            <div class="unit-label">Units Available</div>
                        </div>
                        
                        <div class="d-flex align-items-center justify-content-between mb-3">
                            <div class="d-flex align-items-center">
                                <span class="status-indicator <%= statusClass %>"></span>
                                <small class="text-muted"><%= statusText %></small>
                            </div>
                            <div class="text-end">
                                <small class="text-muted">Last Updated</small><br>
                                <small class="fw-bold">Today</small>
                            </div>
                        </div>
                        
                        <div class="action-buttons">
                            <button class="btn btn-custom btn-update flex-fill">
                                <i class="bi bi-arrow-up-circle me-1"></i>Update
                            </button>
                            <button class="btn btn-outline-primary btn-custom">
                                <i class="bi bi-eye"></i>
                            </button>
                        </div>
                    </div>
                </div>
                <% }) %>
            </div>
        </div>
    </div>

    <!-- Floating Action Button -->
    <a href="/addStock" class="fab" title="Quick Add Stock">
        <i class="bi bi-plus-lg"></i>
    </a>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
